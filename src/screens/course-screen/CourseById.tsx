import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const CourseById = () => {
	return (
		<View>
			<Text>CourseById</Text>
		</View>
	);
};

export default CourseById;

const styles = StyleSheet.create({});
